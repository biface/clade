# .tox-config/requirements/docs.txt — Sphinx documentation build.
#
# #80: django.setup() wired in conf.py — no new package added here, since
# tox installs the project itself (core dep `django>=5.2`) into every env
# by default, docs included. This file only lists what's NOT already a
# project dependency: sphinx, furo, myst-parser.
#
# NOTE: this file is consumed by `tox -e docs` only. It is NOT exposed as
# a pip extra (`pip install ".[docs]"`) — pyproject.toml has no `docs`
# extras group. Deferred to #87 (.readthedocs.yaml): RTD will either
# install directly from this file, or a `docs` extra will be added then.
# Decision not yet made — see #87.
#
# Refs: DD-017 (#78), #79, #80

sphinx
furo
myst-parser
