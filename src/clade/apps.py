from django.apps import AppConfig


class CladeConfig(AppConfig):
    """Application configuration for the clade Django module."""

    name = "clade"
    verbose_name = "Clade"
    default_auto_field = "django.db.models.BigAutoField"
